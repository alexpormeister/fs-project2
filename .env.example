# Copy this file to .env and fill in values for local development.
# DO NOT commit .env. Hosting platforms like Render use their own Environment tab.

PORT=3000                       # Port number for local testing.
DATABASE_URL=postgres://USER:PASSWORD@HOST:5432/DB_NAME   # Student database connection string.
JWT_SECRET=change-me-in-local-dev                        # Secret for signing tokens/sessions.
API_KEY_OMDB=your-omdb-key-here                          # Example of third-party API key (if needed).
